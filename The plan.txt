1. Add user authentication.
   a. repurpose role based access control
   b. jwt tokens
   c. forgot password
   d. password verification via email
   e. expiration

2. Document Management System:
   b. some portions of the document will be dynamically populated (eg when payment is done, when registration through input form, checkbox, when processing a receipt and generation of id or qr code         	if necessary)
   c. flexibly call these templates in places where they might be needed.